{
  "name": "aikata-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "シロ(Aikata)バックエンド — Cloudflare Workers + D1 版(全Cloudflare・無料・永続)",
  "scripts": {
    "build:frontend": "npm --prefix ../frontend run build",
    "dev": "wrangler dev",
    "pretest": "npm run cf-typegen",
    "test": "vitest run",
    "test:watch": "vitest",
    "pretypecheck": "npm run cf-typegen",
    "typecheck": "tsc --noEmit",
    "db:init:local": "wrangler d1 execute aikata --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute aikata --remote --file=./schema.sql",
    "db:migrate-v3:local": "wrangler d1 execute aikata --local --file=./schema_v3.sql",
    "db:migrate-v3:remote": "wrangler d1 execute aikata --remote --file=./schema_v3.sql",
    "deploy": "npm run build:frontend && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "~5.6.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.101.0"
  }
}
